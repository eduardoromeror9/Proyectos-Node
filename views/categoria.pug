extends ./layout/app

block contenido


  section(class="py-5")
    h2(class="text-center text-3xl font-extrabold pt-10 text-gray-700")= pagina

    if propiedades.length
      div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
        each propiedad in propiedades
          include ./templates/card

    else
      p(class="text-center text-sm py-10 text-gray-600") No hay propiedades en esta categoría